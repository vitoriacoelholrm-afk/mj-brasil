# MJ Brasil

A client app composed from the Astralitics catalog on the standard chassis.
Run `pnpm install && pnpm dev`. Modules are installed/updated from Astralitics via the
`astralitics` CLI — do not hand-edit `apps/web/src/modules/*` (managed cells).
